# Drama dash

```powershell
.\Drama-Dash.ps1                       # play (needs a ~70x19 console)
.\Drama-Dash.ps1 -NoColor              # plain mode if ANSI looks garbled
.\Drama-Dash.ps1 -Silent               # no beeps
.\Drama-Dash.ps1 -Storyboard -Seed 7   # the static montage above
.\Drama-Dash.ps1 -Demo 240 -Seed 3     # headless autopilot (what I used to test)
```
